# LeafLock Environment Variables for Coolify
# Copy these variables to your Coolify project environment variables section
# Generate secure values using the commands provided below

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

# PostgreSQL password (REQUIRED: Use openssl rand -base64 32)
POSTGRES_PASSWORD=

# ============================================================================
# REDIS CONFIGURATION  
# ============================================================================

# Redis password (REQUIRED: Use openssl rand -base64 32)
REDIS_PASSWORD=

# ============================================================================
# SECURITY KEYS (CRITICAL: Change these for production)
# ============================================================================

# JWT Secret Key (REQUIRED: Must be 64+ characters)
# Generate with: openssl rand -base64 64
JWT_SECRET=

# Server Encryption Key (REQUIRED: Must be exactly 32 characters)  
# Generate with: openssl rand -base64 32 | head -c 32
SERVER_ENCRYPTION_KEY=

# ============================================================================
# CORS AND FRONTEND CONFIGURATION
# ============================================================================

# Allowed CORS origins (your frontend domain)
# Example: https://leaflock.app,https://*.leaflock.app (apex + previews)
# Note: Wildcards never cover the bare domain; keep both entries.
CORS_ORIGINS=https://leaflock.app,https://*.leaflock.app

# Frontend API URL (your backend domain)
# Example: __ORIGIN__ (use current domain) or https://leaflock.app
VITE_API_URL=__ORIGIN__

# ============================================================================
# APPLICATION SETTINGS (Optional)
# ============================================================================

# Application environment (set to preview/development for PR stacks)
APP_ENV=production

# Enable/disable new user registration (true/false)
ENABLE_REGISTRATION=true

# Enable admin panel in frontend (true/false)
VITE_ENABLE_ADMIN_PANEL=false

# Admin user IDs (comma-separated list of user UUIDs)
# Example: 123e4567-e89b-12d3-a456-426614174000,987fcdeb-51a2-43d7-9f12-345678901234
ADMIN_USER_IDS=

# ============================================================================
# COOLIFY DEPLOYMENT INSTRUCTIONS
# ============================================================================
#
# 1. Copy each variable name and value to Coolify's Environment Variables section
# 2. Generate secure values using the commands provided
# 3. Never use default/example values in production
# 4. Set your actual domains in CORS_ORIGINS and VITE_API_URL
# 5. Keep these values secure and never commit to version control
#
# Commands to generate secure values:
#
# PostgreSQL Password:
#   openssl rand -base64 32
#
# Redis Password:
#   openssl rand -base64 32
#
# JWT Secret (64+ characters):
#   openssl rand -base64 64
#
# Server Encryption Key (exactly 32 characters):
#   openssl rand -base64 32 | head -c 32
#
# Example values for testing (NEVER use in production):
# POSTGRES_PASSWORD=YourSecurePostgresPassword123!
# REDIS_PASSWORD=YourSecureRedisPassword123!
# JWT_SECRET=YourVeryLongJWTSecretKeyThatIsAtLeast64CharactersLongForSecurity123!
# SERVER_ENCRYPTION_KEY=YourExactly32CharacterEncryptKey!
# CORS_ORIGINS=https://leaflock.yourdomain.com
# VITE_API_URL=https://api.leaflock.yourdomain.com
#
# ============================================================================
