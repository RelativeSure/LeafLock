# Test files with hardcoded localhost test database URLs (false positives)
backend/Makefile
backend/*_test.go
backend/coverage.html
scripts/*.sh
.github/workflows/*.yml

# These are test database connection strings pointing to localhost
# Not actual secrets or production credentials
