# Default: exclude everything so each service must opt-in
**

# Backend service build context
!backend/
!backend/**
backend/main
backend/entrypoint
backend/*.out
backend/*.log
backend/coverage.*
backend/node_modules

# Frontend service build context (allow builds rooted in frontend/ to work)
!frontend/
!frontend/**
frontend/node_modules
frontend/dist
frontend/.cache

# Docs (not used in Docker builds but keep the option open)
!docs/
!docs/**
 docs/node_modules
 docs/dist
 docs/.astro

# Generic junk
**/*.log
**/*.tmp
**/*.swp
