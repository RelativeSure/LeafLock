/** @type {import('tailwindcss').Config} */
export default {
  // In v4, content detection is automatic from Vite and build tools
  // No need to specify content paths manually
  
  // Plugins can still be configured if needed
  plugins: ['@tailwindcss/typography']
}